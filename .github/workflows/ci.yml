name: CI

on:
  pull_request:
  push:
    branches: [develop, main]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
      worker: ${{ steps.filter.outputs.worker }}
      preview_worker: ${{ steps.filter.outputs.preview_worker }}
      terraform: ${{ steps.filter.outputs.terraform }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            web:
              - '.github/workflows/**'
              - 'apps/web/**'
              - 'packages/core/**'
            worker:
              - '.github/workflows/**'
              - 'apps/worker/**'
              - 'packages/core/**'
            preview_worker:
              - '.github/workflows/**'
              - 'apps/preview-worker/**'
            terraform:
              - '.github/workflows/**'
              - 'infra/terraform/**'
      - name: Write dev deployment change flags
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/develop' }}
        run: |
          mkdir -p dev-change-flags
          {
            echo "web=${{ steps.filter.outputs.web }}"
            echo "worker=${{ steps.filter.outputs.worker }}"
            echo "preview_worker=${{ steps.filter.outputs.preview_worker }}"
            echo "terraform=${{ steps.filter.outputs.terraform }}"
          } > dev-change-flags/flags.env
      - uses: actions/upload-artifact@v4
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/develop' }}
        with:
          name: dev-change-flags
          path: dev-change-flags/flags.env
          retention-days: 1

  web:
    needs: changes
    if: ${{ needs.changes.outputs.web == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    env:
      MONGODB_URI: mongodb://127.0.0.1:27017/mymediavault
      MMV_MONGODB_DB_NAME: mymediavault
      AUTH_SECRET: test-secret
      AUTH_MICROSOFT_ENTRA_ID_ID: test-client-id
      AUTH_MICROSOFT_ENTRA_ID_SECRET: test-client-secret
      AUTH_MICROSOFT_ENTRA_ID_ISSUER: https://login.microsoftonline.com/test-tenant/v2.0
      MMV_ADMIN_OBJECT_IDS: '[]'
      MMV_ADMIN_GROUP_OBJECT_IDS: '[]'
    steps:
      - uses: actions/checkout@v5
      - uses: supercharge/mongodb-github-action@1.12.0
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: |
            apps/web/package-lock.json
            packages/core/package-lock.json
      - run: npm --prefix ../../packages/core ci
      - run: npm ci
      - run: npm run build
      - run: npm run test

  worker:
    needs: changes
    if: ${{ needs.changes.outputs.worker == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/worker
    env:
      MONGODB_URI: mongodb://127.0.0.1:27017/mymediavault
      MMV_MONGODB_DB_NAME: mymediavault
      MMV_TORRENT_PROVIDER: fake
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: |
            apps/worker/package-lock.json
            packages/core/package-lock.json
      - run: npm --prefix ../../packages/core ci
      - run: npm ci
      - run: npm run build
      - run: npm run test

  preview_worker:
    needs: changes
    if: ${{ needs.changes.outputs.preview_worker == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/preview-worker
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --locked
        env:
          UV_INDEX_LINGXT_USERNAME: az
          UV_INDEX_LINGXT_PASSWORD: ${{ secrets.uv_index_lingxt_password }}
      - run: uv run pytest

  terraform:
    needs: changes
    if: ${{ needs.changes.outputs.terraform == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra/terraform/envs/dev
    env:
      TF_VAR_app_image: docker.io/example/mymediavault:validate
      TF_VAR_worker_image: docker.io/example/mymediavault-worker:validate
      TF_VAR_app_commit_sha: validate
      TF_VAR_auth_secret: test-auth-secret
      TF_VAR_entra_tenant_id: ${{ vars.AUTH_MICROSOFT_ENTRA_ID_TENANT }}
      TF_VAR_auth_entra_client_id: ${{ vars.AUTH_MICROSOFT_ENTRA_ID_ID }}
      TF_VAR_auth_entra_client_secret: test-entra-client-secret
      TF_VAR_admin_object_ids: ${{ vars.TF_VAR_ADMIN_OBJECT_IDS || '[]' }}
      TF_VAR_admin_group_object_ids: ${{ vars.TF_VAR_ADMIN_GROUP_OBJECT_IDS || '[]' }}
    steps:
      - uses: actions/checkout@v5
      - uses: hashicorp/setup-terraform@v3
      - name: Check formatting
        run: terraform fmt -check -recursive ../..
      - name: Initialize without backend
        run: terraform init -backend=false
      - name: Validate
        run: terraform validate

  result:
    if: ${{ always() }}
    needs:
      - changes
      - web
      - worker
      - preview_worker
      - terraform
    runs-on: ubuntu-latest
    steps:
      - name: Check required results
        shell: bash
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          WEB_RESULT: ${{ needs.web.result }}
          WORKER_RESULT: ${{ needs.worker.result }}
          PREVIEW_WORKER_RESULT: ${{ needs.preview_worker.result }}
          TERRAFORM_RESULT: ${{ needs.terraform.result }}
        run: |
          test "$CHANGES_RESULT" = "success"

          for result in "$WEB_RESULT" "$WORKER_RESULT" "$PREVIEW_WORKER_RESULT" "$TERRAFORM_RESULT"; do
            case "$result" in
              success|skipped) ;;
              *) exit 1 ;;
            esac
          done
