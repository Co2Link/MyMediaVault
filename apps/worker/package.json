{
  "name": "mymediavault-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm --prefix ../../packages/core run build",
    "build": "tsc -p tsconfig.json",
    "manual-worker": "node dist/manual-worker.js",
    "pretest": "npm --prefix ../../packages/core run build",
    "test": "vitest run",
    "event-worker": "node dist/event-worker.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.850.0",
    "@mymediavault/core": "file:../../packages/core",
    "mongoose": "^8.20.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.5"
  }
}
